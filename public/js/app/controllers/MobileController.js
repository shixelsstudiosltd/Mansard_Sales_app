define(['Mansard', 'backbone', 'marionette'],
    function (Mansard, Backbone, Marionette) {
    return Backbone.Marionette.Controller.extend({
        initialize:function (options) {
        },
        //gets mapped to in AppRouter's appRoutes
        index:function () {
        }
    });
});